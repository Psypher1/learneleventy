{% set previousPost = collections.post | getPreviousCollectionItem(page) %}
{% set nextPost = collections.post | getNextCollectionItem(page) %}

<div class="flex justify-around">
<p>{% if previousPost %}<b>Previous</b>: <a class='text-sky-600' href="{{ previousPost.url }}">{{ previousPost.data.title }}</a>{% endif %}</p>

<p>{% if nextPost %}<b>Next</b>: <a class='text-sky-600' href="{{ nextPost.url }}">{{ nextPost.data.title }}</a>{% endif %}</p>
</div>